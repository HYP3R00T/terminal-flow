[project]
name = "terminal-flow"
version = "0.0.1"
description = "A Python-first runtime framework for building conversational, AI-native terminal applications."
readme = "README.md"
authors = [{ name = "Rajesh", email = "hyperoot.tech@proton.me" }]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
terminal-flow = "terminal_flow:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
