# ruff format

`uv run ruff format` rewrites files in place. Always run this first - it
eliminates noise before lint check runs.

No manual intervention needed. If it fails, check for syntax errors first.
